<h1>Welcome to Code Snippets!</h1>
<br>
<h2>Snippets</h2>
<% @languages.each do |l| %>
  <div class="language_link">
    <%= link_to "#{l.name} (#{l.snippets.count})", snippet_list_path(l)%>
  </div>
<% end %>
